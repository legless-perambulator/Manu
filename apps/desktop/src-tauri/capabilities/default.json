{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window (core commands only).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
