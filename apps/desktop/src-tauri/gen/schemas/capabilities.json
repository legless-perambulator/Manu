{
  "default": {
    "identifier": "default",
    "description": "Default capability for the main window (core commands only).",
    "local": true,
    "windows": ["main"],
    "permissions": ["core:default"]
  }
}
